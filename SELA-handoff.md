# SELA-handoff.md｜CCM Manual 跟進 Kit V1.25.0 — 第三份 handoff

> **本檔給 Kit Claude 用**——升 Kit 時看這份就能高效規畫，不用挖整份 CLAUDE.md（4,600+ 行）。
>
> **產出時機**：CCM Manual 跟進 Kit V1.25.0（鐵律 #0；Kit 從 V1.21.0 跳 4 個 b 版）。
>
> **本專案背景**：MkDocs + Material Theme 文檔網站，給彰濱秀傳癌症中心個管師使用。Sela 個人帳號（sela1227）維護、GitHub Pages 私有 repo + 自訂 Actions workflow 部署。累積 30+ 版本、坑 58 條、規則 18 條。首份 handoff 於首次對齊 Kit V1.9.0 產出、第二份於跟進 V1.21.0 產出、本份為第三份。

---

## ⚠ 先講一件事：handoff v2 的建議在 V1.25.0 中未見收錄

第二份 handoff（跟進 V1.21.0 時產出）提了 6 項建議，對照 Kit V1.25.0 全文 grep 後**都不在**：

| 建議 | V1.25.0 現況 |
|---|---|
| 坑「優化體檢先做技術棧適用性篩選」| 未見 |
| 坑「多次 str_replace 拆段比一次大段替換穩定」| 未見 |
| claude-init 第二章加「技術棧匹配性篩選」| 未見 |
| OPT-5「批次改 markdown 用 assert 而非 if」| 優化庫仍停在 OPT-1~4 |
| sela-philosophy 加「角色化內容寫作」| 未見 |
| claude-init 加「識別已實質落實的對應」| 未見 |

對照組：**首份 handoff 的建議有收**（Kit 坑 #48 環境重置、#49 歷史註解版本號，V1.10.0 收錄）。

**推測**：v2 產出後沒被帶進升 Kit 的對話（handoff 的價值取決於「有沒有被帶過去」這個人工動作）。**這本身可能是 Kit 值得處理的問題** —— 見第四節建議。

本份 handoff **重申上述 6 項**（本輪又累積到新證據），並新增下方第二、三節的內容。

---

## 一、跟進 Kit V1.25.0 的整體感受

### 預期外的順利

- **V1.25.0 新增「已對齊過、但 Kit 已跳版（落差 ≥ 3 個 b 版）」情境，精準命中本專案**。CCM 從 V1.21.0 跳到 V1.25.0（4 個 b 版），若照舊規則「已對齊過 → 不主動回頭改」，**Kit 坑 #67 就會漏掉**（見下）。這條情境補得非常對。
- **Kit 坑 #67 回頭咬到 CCM，而且真的中了**：CCM 的 `deploy.yml` 是 V3.x 時代自己寫的，`cancel-in-progress: false` + 缺 `.nojekyll`——正是 #67 描述的狀態。**這是「跳版跟進」第一次為 CCM 帶來具體修正**，證明這個情境的價值不是理論的。
- **§10 交付閉環（V1.23.0）的設計理由說服力很強**：「生成端的規則遵循是機率性的，檢核端的逐條核對才接近確定性」——這跟 OPT-4「生成完不是結束、是驗證的開始」是同一個洞察的不同層級（OPT-4 管內容正確、§10 管規範遵循）。CCM 本版把它落成規則 18。

### 預期外的卡住

- **Kit 沒說「跳版跟進」時 handoff 要怎麼處理**：是改寫（覆蓋）還是新增一份？CCM 前兩份都是覆蓋 `SELA-handoff.md`——結果是**v2 的內容連同它未被採納的建議一起被本份覆蓋掉了**（本份靠重申搶救）。若 Kit 期待累積式，應明說檔名策略（如 `SELA-handoff.md` 保留最新 + `handoff-archive/`）。
- **優化體檢對純靜態文檔網站仍然 75% 空轉**（v2 已反映、V1.25.0 未處理）：OPT-1/2/3 都是後端/DB/外部呼叫，CCM 全不適用；只有 OPT-4 適用且早已落實。每次跟進都要重跑一次同樣的「不適用」結論。
- **「UI 版號」鐵律在多產物專案沒說清楚**：CCM 站台底下有 3 個獨立產物——MkDocs 主站、`drug-lookup/`（外部系統複本、有自己的版號）、本版新增的 `lung-nodal-map/`（教材）。哪些要掛 CCM 版號？Kit SPEC §10.6 只講「logo 或 app 名稱旁」，沒有多產物的分工原則。CCM 自行判斷：**同一專案內產出的都掛 CCM 版號，外部系統複本掛它自己的版號**——這個判準可能值得進 Kit。

---

## 二、發現的「跨專案通用坑」（建議進 Kit）

### 強烈建議加坑

#### 1. 臨床工具的「準則版本」要跟資料版本一樣納入時效性盤點

- **症狀**：同一站台三份文件用三個不同準則版本並存且互相矛盾——C1 肺癌分期表寫「TNM 第 8 版」（N 只到 N0-N3）、本版新增的淋巴分區圖譜依「AJCC 第 9 版（2025）」（N2 已細分 N2a/N2b）、G2 品質報告的個管實務對照**早就在用 `pN2a`/`pN2b`**（也就是第 9 版）。個管師照 C1 讀報告會看不懂 `pN2a`。
- **原因**：既有的時效性規範（CCM 規則 15、Kit 坑 #52「健保條文每季修訂」）只盤點**資料版本**（健保條文、補助辦法、公告日期），沒把**臨床準則版本**（AJCC / NCCN / WHO 分類）當成同一類東西盤。準則改版比健保條文慢但影響更深——它會改變**判讀語言本身**。
- **做法**：
  - 臨床工具的每份文件在引用準則處標明**版本 + 年份**（「AJCC 第 8 版」而非「TNM 分期」）
  - 新內容引入較新準則時，**不要默默把舊文件改成新版**——標示落差 + 列為待醫師確認項。**準則版本是臨床決定，不是文件決定**
  - 把「準則版本年度檢視」寫進時效性盤點清單（跟資料版本並列）
- **影響範圍**：所有臨床/法規類工具（CCM Manual、Cancer Navigation、OncoPA、健保藥物速查、RTO-QCC…）
- **跟既有坑的差別**：Kit 坑 #51「NCCN Cat 1 ≠ 健保給付」講的是**兩個體系的落差**；#52 講**資料版本的時效**；本條講**同一體系內準則改版造成的文件內部矛盾**，三者互補。
- **證據**：CCM V4.2.0（AJCC 8 vs 9，同站台三處不一致）。

#### 2. 一個專案多個獨立產物時，UI 版號要先定「誰掛誰的版號」

- **症狀**：CCM 站台底下有 MkDocs 主站 + `drug-lookup/`（另一個 SELA 專案的複本，有自己的 v4.1.0）+ `lung-nodal-map/`（本專案教材）。要滿足 SPEC §10.6「UI 版號 = zip 檔名版號」時，不確定該給哪些產物掛 CCM 版號——全掛會讓 drug-lookup 同時顯示兩個版號、都不掛則使用者在教材頁看不到版本。
- **原因**：SPEC §10.6 預設「一個 zip = 一個 app = 一個版號」，沒覆蓋「一個 zip 內含多個可獨立開啟的產物」。
- **做法**：判準是**「這個產物是不是本專案產出的」**——
  - 本專案產出的（教材、子頁、獨立工具頁）→ **掛本專案版號**，並列進版號同步清單（CCM 規則 1 因此從 5 處擴為 6 處）
  - 外部系統的複本（另一個 SELA 專案匯入）→ **保留它自己的版號**，不要蓋成本專案版號（否則來源版本追不回去）
  - 升版腳本要涵蓋所有「本專案產出」的版號位置，**升完 grep 驗證**（呼應 OPT-4）
- **影響範圍**：所有「一個 repo 裝多個可獨立開的頁面/工具」的專案（文檔網站、入口站、教材集合）
- **證據**：CCM V4.2.0（規則 1 從 5 處 → 6 處，含新教材）。

### 重申 handoff v2（本輪再度取得證據）

- **「多次 str_replace 拆段比一次大段替換穩定」**：本輪把所有批次改動改成「每件獨立 try/except + 小段 replace」，**6 段全過、0 中斷**；對照 v2 那輪一次大段失敗、連帶 3 件沒執行。證據強度已達 2 案。
- **OPT-5「批次改 markdown 用 assert 而非 if 靜默漏掉」**：本輪 4 個階段全用 `assert` + 結果逐條印出，靜默漏掉 = 0。

---

## 三、發現的「跨專案設計模式」（建議進規範）

### 1. 「教材 → 指標」對接：讓教學內容掛在待改善的量化缺口上

- **本案發生情境**：肺淋巴分區圖譜教的是「哪幾站算 N2」；本院 114 年核心指標「臨床 IB-II 期縱膈淋巴結取樣 ≥ 3 站」**實際 75%、未達 80% 閾值**（G2 有數據）。把教材從 C1（癌別檔）、G（指標定義）、G2（本院成績）三個入口都指過去——**個管師在「發現缺口」的當下就能點到「補缺口的知識」**。
- **可推廣的原則**：教材不要只掛在「主題」下，也要掛在**「量化缺口」**旁。有品質指標/KPI 的專案，每份教材都問一句「**它對應哪個未達標的數字？**」——對得上的，從那個數字的所在頁面加入口。
- **代價 / 取捨**：需要專案裡真的有量化指標可掛；沒有指標體系的專案（純工具、純參考）不適用。指標更新時 cross-ref 描述（如「75%」）要跟著改 → 屬時效性內容。
- **建議寫入**：`conventions/sela-philosophy.md` 或 start-project-decisions——適用有 KPI/品質指標的業務系統。

### 2. 重申 v2：角色化內容寫作 + 識別已實質落實的對應

（內容見 handoff v2；本輪 C1/F2/G/G2 的 cross-ref 分工再次驗證「single source of truth + 入口點」寫法有效。）

---

## 四、Kit 該調整的地方

### 1. handoff 的「接力」機制沒閉環（本份最重要的一條）

- **現狀**：鐵律 #0 管的是「**產出** handoff」，`claude-init.md` 對齊 SOP 也只到「產出」。但 handoff 的價值 100% 取決於「**下次升 Kit 時有沒有被帶進那個對話**」——這是純人工動作，沒有任何機制保證。
- **證據**：v1 建議被收（2 條坑進 V1.10.0）、v2 建議 6 項全部未見於 V1.25.0。差別很可能就是「有沒有被帶過去」。
- **建議**：
  - Kit master CLAUDE.md 加一條「**升 Kit 的 SOP**」（現在只有「開新專案」跟「對齊既有專案」兩條路徑，**沒有「升 Kit 本身」這條**）：開頭第一件事是「**跟 SELA 要各專案最新的 SELA-handoff.md**」
  - 或在 Kit README changelog 維護一張「**待處理的 handoff 建議**」表，收到就登記、處理完打勾——讓建議不會因為沒被帶進對話而消失
  - 明訂 handoff 檔名策略：覆蓋（現況）還是累積？覆蓋的話，**未採納的建議會被下一份洗掉**（本份靠重申搶救，不可持續）

### 2. 優化體檢加「技術棧適用性前篩」（v2 已提，再提一次）

`claude-init.md` 第二章優化體檢步驟前加第 0 步：

```
0. 技術棧適用性前篩（避免逐條對「不適用」OPT）：
   - 純靜態網站 / 文檔網站 → 跳 OPT-1/2/3（後端排程 / DB / 外部呼叫）
   - 純前端 SPA → 跳 OPT-1
   - 後端 API / 有 DB → 全部要看
   體檢報告先寫一行「N 條中 X 條技術不適用、實際對照 Y 條」，再列建議
```

### 3. 仍無「文檔網站」reference（v1、v2 都提過）

V1.25.0 有 `reference-static-pages`（純前端 SPA，萃取自健保藥物），但 MkDocs/Sphinx/Docusaurus 這類**有 build step 的文檔網站**仍無對應路徑（nav 結構、anchor slug、`build --strict` 當煙霧測試、中文分詞配置都是這類專案特有）。CCM 走「對齊既有專案 SOP」時無 reference 可參考，全靠自行判斷。**若要做，CCM Manual 是現成的萃取來源。**

---

## 五、留在這個專案、不要回流 Kit 的東西

- **CCM 規則 1 的六處版號清單**：MkDocs + JS + CSS + 教材 HTML + README + CLAUDE.md 特化。**原則「版號位置要列舉 + 升完 grep 驗證」可進 Kit，具體清單留專案。**
- **CCM 規則 2-17**：MkDocs / Material Theme / 醫療文檔特化
- **規則 18 的「CCM 專用核對速查表」**：本專案性質決定的常態答案（規則 18 的**做法**來自 Kit §10，速查表本身留專案）
- **肺淋巴分區圖譜內容**（IASLC 站別、N 分期規則）：醫療專業內容
- **C 系列 13 癌別檔結構、CDK4/6 教材、G2 品質報告、MDT 主檔對齊、個管師 A/B/C 匿名**：全部醫療 / 醫院內部運作

---

## 六、Kit Claude 的建議行動清單

### 建議升 Kit 版本

**V1.26.0**（b+1：新增坑 + 新增 SOP 段落 + 新 OPT）

### 必做

- [ ] 新增坑「臨床工具的準則版本要納入時效性盤點」（本檔第二節 #1；跟 #51 #52 互補，臨床專案多）
- [ ] 新增坑「多產物專案的 UI 版號歸屬判準」（本檔第二節 #2；補 SPEC §10.6 的缺口）
- [ ] **Kit master CLAUDE.md 加「升 Kit 的 SOP」**，第一步 = 跟 SELA 要各專案最新 handoff（本檔第四節 #1）
- [ ] 明訂 handoff 檔名 / 累積策略（覆蓋 vs 歸檔）
- [ ] `claude-init.md` 優化體檢加第 0 步「技術棧適用性前篩」（v2 已提）
- [ ] 新增 **OPT-5**「批次改 markdown / 配置：用 `assert old in text` 而非 `if old in text`」（v2 已提，本輪 2 案佐證）
- [ ] 新增坑「多次 str_replace 拆段比一次大段替換穩定」（v2 已提，本輪 2 案佐證）

### 建議做

- [ ] `sela-philosophy.md` 加「角色化內容寫作」（v2 已提）
- [ ] `sela-philosophy.md` 加「教材掛在量化缺口上」（本檔第三節 #1）
- [ ] `claude-init.md` 對齊 SOP 加「識別已實質落實的對應」（v2 已提，避免重複立法）

### 暫緩

- [ ] 「MkDocs / 文檔網站 reference」——三份 handoff 都提了，等 Sela 決定要不要做（CCM Manual 是現成萃取來源）

### 不做

- [ ] CCM 規則 1-18 整套進 Kit（本專案特定）
- [ ] 醫療專業內容（C 系列、CDK4/6、G2、肺淋巴圖譜）

---

## 七、給 Kit Claude 的最後備註

本輪最值得注意的一件事：**Kit 坑 #67 從 OncoPA 反饋進 Kit V1.24.0，接著在 CCM 跟進 V1.25.0 時回頭修好了 CCM 的部署設定**。這是 Kit 第一次對 CCM 產生「反向影響」——過去都是 CCM 貢獻坑給 Kit（#48、#49）。**雙向流通真的成立了。**

但同一輪也看到反面：**handoff v2 的 6 項建議沒有進 Kit**。機制的兩端一強一弱——「坑往上游流」有效（因為 Kit Claude 會讀 pitfalls），「建議往上游流」失效（因為沒人保證 handoff 被帶進升 Kit 的對話）。**第四節 #1 是本份 handoff 最想解決的問題。**

---

**文件資訊**

| 項目 | 內容 |
|------|------|
| handoff 版本 | 3.0（第三份，覆蓋第二份）|
| 對應專案版本 | CCM Manual V4.2.0 |
| 對應 Kit 版本 | V1.25.0 |
| 前份未採納建議 | 已於本份「⚠ 先講一件事」與第六節重申 |
| 維護單位 | Sela（個人）|
