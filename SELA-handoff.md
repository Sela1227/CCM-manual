# SELA-handoff.md｜CCM Manual → Kit　第五份 handoff（v5.1）

> **給 Kit Claude**：升 Kit 時看這份就好，不用挖 CCM 的 CLAUDE.md（1,900+ 行）。
>
> **本專案**：MkDocs + Material 文檔網站，給彰濱秀傳癌症中心個管師用。Sela 個人帳號（sela1227）維護、GitHub Pages 私有 repo + 自訂 Actions 部署。累積 40+ 版本、**坑 72 條**、規則 18 條。**已對齊 Kit V1.26.0**。
>
> **v5.1 說明**：v5（產於 V4.13.1）**第零節的核心論斷是錯的，本版全部撤回**——見下。本份已依 **Kit V1.26.0 的分段鐵律**改寫（`#### 本次新增` / `#### 歷史保留`）。

---

## 🔴 第零節：v5 的指控我撤回——「Kit 版本沒動」不等於「沒人讀」

**這一節不是要 Kit 做什麼，是要留下一個更正紀錄。**

### 我在 v5 說了什麼

v5 第零節寫了這些，**每一句都是錯的或過度推論**：

| v5 的話 | 事實 |
|---|---|
| 「handoff 機制**第三次失效**」 | ❌ **機制沒失效**——Sela 有 **SELA-Handoff-Scanner** 定期掃描、批次帶進 Kit 維護對話**逐條處理**，採納／暫緩／不做**都有紀錄** |
| 「**產出端做滿分，接收端是零**」 | ❌ 接收端不是零，是**有帳本的**（`conventions/pending-candidates.md`）|
| 「『**產出 handoff**』是一個**假的完成訊號**」 | ❌ 反了——**產出 handoff 就是正確且足夠的動作**，後面的事本來就不歸專案端管 |
| 「這是六條坑的**第七條**」 | ❌ 這個類比整個垮掉——它建立在上面三句之上 |
| 「**如果只能做一件事，請做**『升 Kit SOP』」 | ❌ 這一項 Kit **已結案不採納**，理由完全成立（見下）|

### 我錯在哪（這才是這一節的價值）

**我唯一的證據是「Kit 版本號從 V1.25.0 一直沒動」，然後我從它推論出「沒人讀 handoff」。**

但那個證據**只證明「Kit 版本號沒動」**。它不能證明「沒被讀到」——**「我觀察不到證據」不等於「事情沒發生」。**

而真相是 Kit V1.26.0 自己寫出來的，**比我的指控更精確**：

> 觸發點：07-17 桌機批次帶回 CCM Manual v4（第零節指控「handoff 流不上去」——**屬實**：其 v2/v3.1 建議在 **07-03 快篩中被誤標已整合**、從未進 Kit）

**所以 v4 的指控「屬實」，但成因跟我以為的完全不同**：不是「沒人讀」，是「**07-03 的快篩把它們誤標成已整合**」。**是一個具體的、可修的流程 bug，不是一個「機制性的冷漠」。**

我的推論鏈是：**沒採納 → 沒讀到 → 機制失效 → 產出端是白工**。**第一步就跳錯了。**

### 🔑 這條要進 Kit（本次新增的坑 #72）

> **「我觀察不到證據」≠「事情沒發生」。** 用「對方沒動作」去反推「對方的內部狀態」（沒讀、不在乎、機制壞了），是把**觀察不到**當成**不存在**。
>
> **這跟 v5 主秀的六連坑是同一族，只是鏡像**：六連坑是「**一個看起來已完成的標記，讓我停止了本該繼續的動作**」；這一條是「**一個看起來未完成的標記（版本號沒動），讓我斷定一件事沒發生**」。
>
> **做法**：**沒有第一手證據就不要寫成指控**。要嘛去問，要嘛把它降級成「我觀察到 X，但我不知道原因」。

### 順帶承認兩個具體錯誤

1. 🐛 **v5（含 v4）的 A-4 說「跟 Kit 既有坑 #55 角色化內容寫作同源」——編號記錯了**。Kit 已抓到：**#55 實為「字串切片吞夾層定義」**（從 MapQuiz）。**我提案時沒 grep Kit 就寫編號**——而 handoff 模板「提案前的兩個檢查」第 1 條就是「先 grep Kit」。**本份所有提案都已 grep 過。**
2. 🐛 **v5 建議、我還自己實作了 `handoff-archive/`——Kit 明確否決且理由更好**（「歷史保留」段已解同一問題、免增檔案面）。**本版已移除 `handoff-archive/`，改用分段鐵律。**

### 「升 Kit SOP」被結案，我同意

Kit 的理由我完全沒想到：

> **升 Kit SOP 的家是 Kit 維護 Project 的 Instructions（Kit 是給專案 Claude 的）**

**對——我提議「把升 Kit 的 SOP 寫進 Kit master CLAUDE.md」，但那份文件的讀者是「專案 Claude」，不是「Kit 維護 Claude」。** 專案 Claude 讀到「怎麼升 Kit」也沒用。**我把 SOP 放在一個它的執行者根本不會讀的地方，還說「如果只能做一件事，請做那一件」。**

---

## 一、整體感受（給 Kit Claude 校準）

### 順利的

- 🏆 **V1.26.0 是 CCM 這條回流路的最大豐收**：**5 條坑（#76–#80）+ coding-style §7 + SPEC §10.6 + claude-init 第 0 步 + philosophy 五.6**，一次全進。加上既有的 #48/#49，**CCM 累計貢獻 7 條 Kit 坑**。
- **「接收端說明」+ 候補帳本正好打中痛點**——而且**比我提的方案好**：我提的是「加一個 SOP」（治標、還放錯地方），Kit 給的是「**說明機制長什麼樣 + 建一本帳**」（治本）。**專案端焦慮的根源是「我不知道我的建議去哪了」，帳本直接解決它。**
- **分段鐵律（`#### 本次新增` / `#### 歷史保留`）解掉了 v4 抱怨的「覆蓋洗掉」**——而且**成本比我提的 archive 資料夾低**。

### 卡住的

- **沒有了。** v4 列的卡點（handoff 流不上去、優化體檢 75% 空轉、文檔網站 reference）**全部有交代**：前兩個已解，第三個在候補帳本等 Sela 提供素材。

---

## 二、發現的「跨專案通用坑」（建議進 Kit）

#### 本次新增

> **全部已 grep 過 Kit V1.26.0，確認無重複。**

##### 🅐 「假的完成訊號」四連（#69／#70／#71／#72）——**建議合成 1 條新坑**

**Kit 目前沒有對應條目**（grep 過 `待確認`／`backlog`／`五分鐘查` 皆無）。

這四條是同一個機制的四種長相：**一個看起來代表「已完成／已查證／已處理」的標記，讓我停止了本該繼續的動作。**

| # | 那個「假的完成訊號」是 | 它其實只擔保 | 我卻以為它擔保 |
|---|---|---|---|
| **#69** | 「⚠️ 待癌登窗口確認」 | 「我標記了一個未知」 | 「這件事處理過了」 |
| **#70** | 候選清單上那一行掛著 | 「它在 backlog 裡」 | 「查不到才會掛著」 |
| **#71** | 「NCCN 需帳號 → 這是 🅑」 | 「**NCCN** 拿不到」 | 「**這個癌別**無法查證」 |
| **#72** | 「Kit 版本號沒動」 | 「版本號沒動」 | 「沒人讀 handoff」 |

- **症狀**：
  - **#69**：癌登採用 AJCC V9，**主管機關早有正式公告**（附文號）。而我從 V4.4.0 一路寫「⚠️ 待窗口確認」寫到 V4.11.0、**還列進候選清單掛了 5 個版本**。權威源的 8 個 ⚠️ 被 13 個檔 cross-ref，**「待確認」這個狀態被複製到全站**。
  - **#70**：Sela 說「把該自己查的那四項查掉」→ **一動手，四項全部有答案**。最痛的是：**健保條文原文一直躺在專案資料夾裡**，我每版都看得到，因為 `pdftotext` 讀不出來就放棄了——**沒試過 `unzip`**（而我的筆記裡就寫著「zip 偽裝的 PDF」）。
  - **#71**（V4.14.0 新增）：我盤點完治療準則，判定「13 個檔只有 1 個是原文全文」「NCCN 需帳號 → 這是要問人的」。**這個結論本身沒錯。** 但改寫某癌別檔時審稿提到另一個學會——**一查，那個學會的指引是免費線上全文、不需帳號、可直接讀完**。該檔當場從「未查證」升到「原文全文」。
  - **#72**（本版新增）：見第零節。
- **共同根因**：**「查不到」和「沒去查」長得一模一樣**；**「沒有動作」和「沒有讀到」長得一模一樣**。這些標記都**有理由、有責任人、看起來處理過了**——它們是**合法的標記**，但變成了**停止動作的藉口**。
- **做法／可推廣的判準**：
  1. 🔑 **標「待確認」之前，先真的花五分鐘查一次**。查不到再標——**而且要寫下「我試過什麼」**。
  2. 🔑 **「待確認」要分兩種**：「**我查不到**」（→ 我的責任，去查）vs「**只有對方知道**」（→ 才是真的要問人）。**寫不出「誰能回答」就代表其實我該自己查。把前者標成後者，等於把自己的工作外包給不會來的人。**
  3. 🔑 **手上的檔案讀不出來 ≠ 沒有答案**——`pdftotext` 失敗只代表「它不是 PDF」。**檔案格式是可以驗的**（`file` 指令三秒鐘）。
  4. 🔑 **「拿不到 A 來源」不是「查不到」，是「還沒找 B 來源」**——**不是每個權威來源都難拿**（有些學會指引免費全文、有些在期刊）。**「權威」和「難拿」沒有必然關係。**
  5. 🔑 **「我觀察不到證據」≠「事情沒發生」**——不要用「對方沒動作」反推「對方的內部狀態」。
  6. **backlog 會製造「已處理」的錯覺**——**每次複製那一行的時候，要問「這一版我能不能把它做掉？」**
  7. **權威源裡的每個 ⚠️ 都要有明確的責任人**（否則會被 cross-ref 放大到全站）。
- **證據**：CCM Manual 坑 #69（V4.12.0）、#70（V4.13.0）、#71（V4.14.0）、#72（V4.14.1）。**四條分佈在連續四個版本，是同一個機制在不同層面重演。**
- **跟 Kit 既有的關係**：跟 **#43（用 assert 不用 if）精神一致**——都是「**別把『沒做』偽裝成『做了』**」，只是一個在程式碼、一個在待辦與認知。

##### 🅑 操作型小條目（可併入上條的「做法」，或獨立）

- **找主管機關的答案，先找執行單位的「操作手冊」，不要只找公告**——CCM 案例：癌症登記中心的《編碼對照與版本差異參考手冊》**一份文件解掉 8 個掛了多版的 ⚠️**，而且它**自己就依適用年度分好了**。**公告告訴你「改了」，操作手冊告訴你「怎麼對照」。**

#### 歷史保留

> 以下為 v4 提案，**Kit V1.26.0 已全數處理**，本節僅列管追蹤、不重講。

| v4 編號 | 提案 | Kit 處理結果 |
|---|---|---|
| A-3 | 審稿建議 = 診斷 + 解方 | ✅ **已進 Kit #76** |
| D-1 | 臨床準則版本納入時效性盤點 | ✅ **已進 Kit #77** |
| C-1 | build / lint 檢查死角 | ✅ **已進 Kit #78** |
| B-1 | 數字型單一真相 | ✅ **已進 Kit #79** |
| A-1 + A-2 | 訓練文件四問法 + 折疊判準 | ✅ **已進 Kit #80**（合併）|
| A-4 | 角色化內容寫作 | ✅ **已進 philosophy 五.6**（我誤指「跟 #55 同源」，編號記錯，已承認）|
| 量化缺口 | 教材掛在量化缺口上 | ✅ **已進 philosophy 五.6** |
| C-2 + C-3 | assert 不用 if、拆小段、每件獨立 try/except | ✅ **已進 coding-style §7**（不開 OPT-5，理由成立：編輯紀律非新舊寫法升級）|
| B-2 | 多產物版號歸屬判準 | ✅ **已進 SPEC §10.6** |
| 建議 3/4 | 優化體檢技術棧前篩 + 識別已實質落實 | ✅ **已進 claude-init 第 0 步** |
| 第零節-1 | 升 Kit SOP 進 master CLAUDE.md | ✅ **已結案不採納，我同意**（見第零節）|
| 第零節-2/3 | handoff 檔名策略 / 待處理建議表 | ✅ **已解**（分段鐵律 + 候補帳本；**我的 archive 方案已撤回**）|
| E-1 | 外部來源產物整合層工具化 | 🕒 **候補帳本，等第二案**（合理，CCM 目前 N=1）|
| — | 「文檔網站」reference（MkDocs 等） | 🕒 **候補帳本，等 Sela 提供 CCM 檔案做萃取素材** → **Sela 決定即可，CCM 這邊隨時可提供** |
| — | 章法舉例補「文檔網站」場景 | 🕒 **候補帳本**（章法手冊原作不可動，理由成立）|

---

## 三、建議進 `sela-philosophy.md` 的設計模式

#### 本次新增

- **（無）**——本輪的東西都是坑，不是哲學層。

#### 歷史保留

- 角色化內容寫作、教材掛量化缺口 → ✅ **已進 philosophy 五.6**。

---

## 四、Kit 該調整的地方

#### 本次新增

| # | 建議 | 說明 |
|---|------|------|
| 1 | 🐛 **`conventions/coding-style.md` 章節編號衝突** | **有兩個 `## 7.`**——原本的「7. 危險操作的確認機制」（第 164 行）與 V1.26.0 新增的「7. Python 批次編輯三紀律」（第 195 行）。而且**新節排在 `## 9. 不做的事` 之後**，順序也亂了。建議新節改 `## 10.` 並移到檔末，或重編。**（這條是 CCM 的提案被採納後產生的，算我這邊順手回報）** |
| 2 | 💡 **`pending-candidates.md` 的「等第二案」可考慮加一欄「誰能提供第二案」** | 現在有 17 條候補，但**沒人知道要等誰**。CCM 的 E-1 若標上「其他有外部產物整合的專案」，Sela 掃描時就能主動配對。**這是 CCM 坑 #69 的判準（「待確認要寫誰能回答」）套用到 Kit 自己身上**——不寫責任人的待辦，會一直躺著。 |

#### 歷史保留

- v4 的 6 項建議 **✅ 全數已處理**（見 §二歷史保留表）。

---

## 五、留在 CCM、不要回流 Kit

- **規則 1 的七處版號清單**（具體位置）——原則已在 SPEC §10.6，清單留專案
- **規則 2–18 的具體條文**——MkDocs / Material / 醫療文檔特化
- **所有醫療專業內容**——癌別檔結構、藥物教材、品質指標、分期版本對照、**來源層級四級制的實際內容**
- **醫院內部運作**——MDT 主檔對齊、個管師匿名、院內流程
- **CCM 跟另一個 SELA 專案的版本號同步策略**——個別決定

---

## 六、Kit Claude 的建議行動清單

### 建議升 Kit 版本

**V1.27.0**（b+1：1 條新坑 + 1 個 bug 修正 + 1 項小補強）
**或 V1.26.1**（c+1：若判定「假的完成訊號」合併進既有坑而非新增條目）

### 必做

#### 本次新增

- [ ] **新坑：「假的完成訊號」——標「待確認」前先花五分鐘查、分「我查不到 vs 只有對方知道」、「拿不到 A 來源 ≠ 查不到」、「觀察不到證據 ≠ 沒發生」**〔4 案佐證，分佈 CCM V4.12.0–V4.14.1 連續四版〕
- [ ] 🐛 **修 `coding-style.md` 兩個 `## 7.` 的編號衝突**〔V1.26.0 引入〕
- [ ] **補強 Kit #78**：加「**工具沒報有三種可能：真的沒問題／工具沒檢查／工具報了但被我自己的過濾器濾掉**」——CCM 坑 #65 是第三種的實例（`grep WARNING|ERROR` 把 `INFO` 級的死 anchor 訊息濾掉，**躺了 7 個版本**）。判準：**能在工具設定層解決的，不要在下游補**（正解是設定檔調報告等級，不是在 grep 加關鍵字）。**#78 目前只涵蓋第二種。**
- [ ] **補強 Kit #77**：加「**盤點之後仍會出錯的三種方式**」——CCM 坑 #66（**標籤是新的、內容是舊的**：標了「依 XX 年公告版」+ 每年提醒，但**當初就寫錯**；改標「**查證日期**」而非「XX 年公告版」）／#67（**訂了規則不等於套用到全站**；**跨兩個分類的東西最容易漏**——某準則同時是「分期系統」和「治療準則」，**兩邊都以為對方管了**）／#68（**「來源是幾手的」跟「來源新不新」一樣重要**——靠二手解讀文章寫內容，**把三個層級的建議壓成一句話**，其中一句還跟原文相反；已落成「**來源層級四級制**」①原文全文／②官方摘要／③二手解讀／④未查證）。
- [ ] 💡 **`pending-candidates.md` 加「誰能提供第二案」欄**〔小補強〕

#### 歷史保留

- [ ] v4 全部項目 ✅ **已於 V1.26.0 處理完畢，無待辦**

### 暫緩

#### 本次新增

- [ ]（無）

#### 歷史保留

- [ ] **E-1 外部來源產物整合層工具化** — 候補帳本等第二案（合理）
- [ ] **「文檔網站」reference** — 候補帳本等 Sela 提供素材。**CCM Manual 隨時可作萃取來源，Sela 說一聲即可**
- [ ] **章法舉例補文檔網站場景** — 候補帳本

### 不做

- [ ] CCM 規則 1–18 整套進 Kit
- [ ] 醫療專業內容
- [ ] ~~升 Kit SOP 進 master CLAUDE.md~~ — **已結案，我同意理由**

---

## 七、最後備註

v4 那句「**坑會被 Kit Claude 主動讀到，handoff 不會**」——**現在也不成立了**：V1.26.0 加了接收端說明、建了候補帳本、鎖了 scanner 的解析格式。**這條路是通的，而且比我以為的更有制度。**

**v5 花了整整一節去建構「機制失效」的故事，還把它包裝成「六條坑的第七條」——那個故事很完整、很有說服力，而且是錯的。** 它唯一的地基是「Kit 版本號沒動」。

**這件事本身比 v5 想講的任何一條坑都更值得留下來**：我很擅長把零散的觀察組織成一個漂亮的因果故事，**而故事的完整度跟它的真實度沒有關係**。

---

**文件資訊**

| 項目 | 內容 |
|------|------|
| handoff 版本 | **5.1**（v5 的第零節已全部撤回並更正）|
| 對應專案版本 | CCM Manual **V4.14.1** |
| 對應 Kit 版本 | **V1.26.0**（已跟進）|
| 產出原因 | 修正 v5 的錯誤論斷 + 依 V1.26.0 分段鐵律改寫 |
| 本份主秀 | **「假的完成訊號」四連（#69–#72）**——含 #72「我觀察不到證據 ≠ 沒發生」，即 v5 第零節犯的錯 |
| v4 狀態 | ✅ **Kit V1.26.0 已全數處理**（5 條坑 + 4 處補強 + 2 項結案）|
| 已撤回 | v5 第零節全部論斷；`handoff-archive/` 方案；A-4 的 #55 編號 |
| 維護單位 | Sela（個人）|
